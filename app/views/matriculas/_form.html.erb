<%= form_with(model: matricula) do |form| %>
  <% if matricula.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(matricula.errors.count, "error") %> prohibited this matricula from being saved:</h2>

      <ul>
        <% matricula.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :aluno_id, style: "display: block" %>
    <%= form.text_field :aluno_id %>
  </div>

  <div>
    <%= form.label :disciplina_id, style: "display: block" %>
    <%= form.text_field :disciplina_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
